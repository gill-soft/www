// import { render } from "@testing-library/react";
import FormForBuy from "../components/TripsContainer/FormForBuy";

export const getForm = (v) => {
  for (let i = 0; i < 3; i++) {
      console.log("object")
    return <FormForBuy key={i} />
  }
};
